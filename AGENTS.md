# Project workflow

After completing a requested change and its appropriate checks, automatically commit the task's
changes and push the current branch to its configured remote. This is the user's standing
preference; no additional commit or push confirmation is needed unless the user overrides it.

Keep unrelated changes out of commits and preserve other work in progress. Do not force-push
shared branches. If verification or pushing is blocked, report the blocker accurately.
